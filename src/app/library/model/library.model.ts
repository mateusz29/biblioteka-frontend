export class Library {
    id: string;
    name: string;
    yearFounded: number;

    constructor(id: string, name: string, yearFounded: number) {
        this.id = id;
        this.name = name;
        this.yearFounded= yearFounded;
    }
}
